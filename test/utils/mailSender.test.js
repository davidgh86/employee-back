const sendMail = require('../../src/utils/mailSender')
test('send mail', async () => {
    if (false)
    sendMail(
        "david_gh86@hotmail.es", 
        `Restore femploy password`,
        `Hola`
      )
});